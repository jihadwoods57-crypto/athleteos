# Fable 5 — Project Memory (OnStandard / athleteos)

## Product Vision
OnStandard is honest athlete nutrition + performance: real habit formation over vanity metrics; coaches are the
buyers, parents pay, athletes are taught (not just scored).

## Features shipped
- 2026-07-12 — **T1 · Camera capture honesty** (branch `fable5/2026-07-12-founder-worklist`, commit `d41fa6f`, tag `fable5/2026-07-12-t1-kill-fake-meal-image`; NOT merged): killed the hardcoded `meal-lunch.jpg` viewfinder background, the fake green "LIVE" pill, and the fake flash/flip `.vf-tool` controls in `camera.js`; replaced with an honest empty state (camera glyph + "Take a photo to analyze" + hint) on new `.vf-empty` CSS reusing existing tokens. Corner guides, `.vf-deadline`, and the shutter→file-input→`captureMeal`→analyzing path untouched. Verify GREEN 143/143 · 1745/1745; live-probed at 390×844 (no jpg backgrounds, no LIVE text, controls ≥44px). Evidence: `.fable5/shots/2026-07-12-worklist/t1-*.png`, report `reports/2026-07-12-founder-worklist.md`.
- 2026-07-12 — **T2 · Gallery + non-live disclosure** (same branch, commit `dce7558`, tag `fable5/2026-07-12-t2-gallery-integrity`; NOT merged): Gallery tile now works via a second `#cam-gallery` input (`accept=image/*`, no `capture` attr) sharing the shutter's downscale/analyze path; gallery picks flagged `live:false` end-to-end (`MEAL.live` → `logMeal` meta → `DAY.slotMacros[slot].live`) and disclosed with shared `components.nonLiveBadge()` (`.status-pill.a`, 12.06:1) + "Live capture only for scored meals" copy on analyzing/analysis/thread. Scoring math untouched — badge is disclosure, not enforcement. Verify GREEN; 8/8 acceptance live-verified at 390×844. Evidence: `t2-*.png` shots.
- 2026-07-12 — **SEC · Authz hardening batch** (same branch, commit `b78f8ec`): CRITICAL `guardianships` self-appoint (any user could appoint themselves guardian of anyone → PII read + minor-messaging bypass) + HIGH `plan_assignments` IDOR closed in **authored-only** migration `0053_authz_hardening.sql` (NOT applied to live — founder applies at go-live after `test:rls`); AI spend fairness caps completed (`analyze-meal` finalize counts, `assist` capped, `plan-generate` fails closed); password policy 8+letters_digits; `verify_jwt` pinned; MIME sniff in analyze-meal. New probes in `rls_authz_test.sql §3b`. Full audit: `docs/SECURITY-AUDIT-2026-07-12.md`. Verify GREEN 1745/1745. (Touched `supabase/**` — outside proto scope, flagged to founder in the run report.)
- 2026-07-12 — **T4 · Profile avatar camera badge** (same branch, commit `06568b2`, tag `fable5/2026-07-12-t4-avatar-camera-badge`; NOT merged): oversized 44px visible badge split into a transparent 44×44 hit box (`#avatar-btn`, `aria-label="Upload photo"`) containing a 28px visible `.req-badge b` circle, icon 12→14. Upload wiring untouched; verify GREEN. Evidence: `t4-*.png` shots.
- 2026-07-12 — **T3 + T5 design gates (no code)**: T3 Home score-box redesign (single score dial, number inside ring, dotted ready-ring at 0, honest red "Off Standard" pill — `.status-pill.r` currently has NO css; variants A dial-first [recommended] / B progress-first) at `reports/2026-07-12-t3-design.md`, prototype https://claude.ai/code/artifact/1d7ec88b-a131-42ab-9aa3-0e5c9be62e2b (commit `402f85f`). T5 Notifications upgrade (full screen not drawer; per-row read/unread never color-alone; Mark-all-read maps to existing `readNotifs()`; variants A by-recency [recommended] / B by-consequence) at `reports/2026-07-12-t5-design.md`, prototype https://claude.ai/code/artifact/61f0ebdb-f9bb-4cd1-a318-92711605b736 (commit `60c6d8a`). AWAITING FOUNDER APPROVAL before any build.
- 2026-07-12 — **plan-a1 · Offline/loading false-negative on Plan targets** (branch `fable5/2026-07-12-founder-worklist`, commit `94b3fc9`; NOT merged): `S.planTargets` collapsed coach-set-nothing / still-hydrating / offline-fetch-failed into the same `null`, so an offline athlete with real coach-set targets was told "Your coach hasn't set targets yet." `_loadProfileIntoRt` (state.js) now destructures the SETTLED `{error}` sentinel from the `athlete_profiles` select, sets `RT.profileLoading`/`RT.profileOffline`, and never clears a cached target on a failed refetch (the patch object only ever writes `RT.profile.targets` when the row actually came back). New `S.planTargetsState` getter resolves 4 honest states with precedence set > loading > offline > unset. plan.js's header subtitle, Today's Objective card, Coach Targets card (shared `.sidebox` loading / `.state-demo` wifiOff+Retry), and Nutrition eyebrow all branch on it; genuinely-unset and already-set copy is byte-identical to before. New `act.retryProfile()` (data-act, router self-wires + re-renders). Verify GREEN (143/143 suites, 1745/1745 tests, expo export OK); all 5 states + the retry recovery path live-verified at 390×844 via a localStorage+mock-supabase-client Playwright harness (deterministic, since the real project has no reachable egress from this sandbox) — no console errors in any state. Screenshots: `.fable5/shots/2026-07-12-worklist/plan-a1-after-*.png`.
- 2026-07-12 — **Screen-by-screen engine, FULL COVERAGE (30/30 ledger rows)** (branch `fable5/2026-07-11-screen-by-screen-frontend-improvement`, 31 commits, tags `fable5/2026-07-12-screens-batch1..5` + `-final`; NOT merged): 40+ improvements across 24 screens — grace-calibrated streak tiers on home/progress/notifications; honest offline on coach/trainer/connect (supabase-js `{error}` sentinel plumbing); real failure states (meal analyzing, camera capture, profile avatar, requirement 404, reset resend, guardian remind); deceptive controls killed (weekly-preview chips, coachVoice toggle, quiet-hours invite); 44px floor swept (recovery chips, plan tabs, foodsearch steppers, ob chrome, .seg, pw-eye, avatar badge); aria semantics (radiogroups, input names, alert regions, humanized notification pills). Verify GREEN after every batch (143/143 · 1745/1745). Full report: `reports/2026-07-12-screen-by-screen-run.md`; per-change QA in `coverage.md`.
- 2026-07-11 — **Streak-at-risk (Home loss-aversion surface)** (branch `fable5/2026-07-11-screen-by-screen-frontend-improvement`, commit `90e1105`, tag `fable5/2026-07-11-systematic-screen-by-screen-frontend-imp`; NOT merged): the passive "🔥 N day streak" pill tiers the moment a 2+ day streak isn't yet counted today — amber "N-DAY · AT RISK" pill + STRONG flame ribbon when `graceUsedRecently` (loss is real), blue "N-DAY · COVERED" pill + MILD shield ribbon when grace is intact. Ribbon is a sibling of `strip()` (data-go isolation), routes to exec now/overdue, self-retires on celebration/day0/<2 days. Matching tiered row leads the notifications feed. Driven 100% by existing `S.streak` (state.js:957-968) / streakInfo (day.js:199) — no new data, backend, or migrations. Verify gate GREEN (143/143 suites, 1745/1745 tests, expo export OK); proto.zip + protoVersion.ts rebuilt; 5 states + routing smoke-tested via localStorage-seeded Playwright. Rationale: chosen over billing-conversion and coach-triage lenses — Home is the daily-return surface every athlete sees, and this reuses an already-honest getter with zero backend risk and never touches the unwired Stripe seam.
- 2026-07-11 — **Practice HQ v1** (branch `fable5/2026-07-11-trainer-profile-practice-hq`, commit `b47d925`, tag `fable5/2026-07-11-redesign-the-trainer-profile-into-a-prem`; NOT merged): trainer profile rebuilt from dead settings page into Practice HQ — server-hydrated real identity (`RT.practice` + `act._loadPracticeIntoRt` + `S.trainerIdentity`), 4-state invite loop (live/loading/minting/offline), dependency-free tested ISO 18004 QR encoder (`src/core/qr.ts`, ported to `proto/redesign-2026-07/js/qr.js`), Copy/Share via native bridge → navigator.share → clipboard, honest LOCKED roadmap rows, and cross-role back-nav fix on coach+trainer tab roots via new `titleHead()`. Verify gate GREEN (135/135 suites, 1679/1679 tests, expo export OK).

- 2026-07-12 — **progress-delta · Direction-aware score delta color (number honesty)** (branch `fable5/2026-07-12-founder-worklist`, commit `6ea3693`; NOT merged): `.bigstat .d` base color changed from hardcoded `--green-bright` to neutral `--text-2`; new `.up`/`.down` modifiers reuse `--green-bright`/`--red` (no new tokens). `progress.js` derives direction from the signed `P.weekDelta` via `parseFloat` (`+0` stays neutral, never green or red). Weight delta (`S.weight.deltaMonth`) intentionally keeps the plain neutral class regardless of sign — direction there is goal-dependent (a gain can be good or bad); the existing `pace` status-pill is the only honest good/bad signal. Verify GREEN 143/143 · 1745/1745; live-verified positive (+13 green), negative (-36 red), zero (+0 neutral) score deltas and a negative weight delta (neutral) at 390×844 via localStorage+live-module-mutation Playwright harness, no new console errors. Screenshots: `.fable5/shots/2026-07-12-worklist/progress-delta-after-*.png`.

- 2026-07-12 — **T2A · Rule A: non-live meals never scored** (branch `fable5/2026-07-12-founder-worklist`, commits `110609f` + QA `89a7538`, tag `fable5/2026-07-12-t2a-nonlive-never-scored`; NOT merged): the founder-approved scoring exception — gallery (`live:false`) meals log/analyze normally but never count toward Nutrition, meals-completion, or streak. Pure `mealScored(day,k)` in day.js gates `effectiveMeals`/`proteinToday`; `projectedDay()` clears `.live` on the reach projection; state.js swaps `mealScored` into all 6 completion contexts + honest "Gallery photo saved — capture live to count" requirements copy; camera.js/meal.js disclose won't-count at 5 copy points BEFORE the athlete commits (+pts block suppressed post-log; meal stays in the trail + coach-visible). Live meals byte-identical (scoreParity 16/16). Verify GREEN 143/143 · 1745/1745; 8/8 acceptance live-verified at 390×844. QA found + fixed one medium contradiction (late non-live "still counts" vs "Won't count", `89a7538`). Evidence: `t2a-*.png`, report `reports/2026-07-12-approved-builds-sweep1.md`.
- 2026-07-12 — **T3 · Score dial build (Variant A + honest red)** (same branch, commit `f4b9438`, tag `fable5/2026-07-12-t3-score-dial-build`; NOT merged): `scoreRing()` gains `centerNum` (number inside the 64px ring), dotted ready-track + spark guard below score 6; home `strip()` drops `.xsc`/`.xmeta` for one dial + "N of TOTAL in · reach POSSIBLE today"; `.status-pill.r` gets real muted-red CSS (#FF9B9B, measured 7.94:1 AA). Presentation only, strip stays the sole `data-go` target (92px). Verify GREEN; 8/8 acceptance live-probed at scores 0/62/82; breakdown hero regression-checked. Evidence: `t3-after-*.png`.
- 2026-07-12 — **T5 · Notifications zero-new-data slice** (same branch, commit `324bdc0`, tag `fable5/2026-07-12-t5-notifications-slice`; NOT merged): coarse read model only (rows unread until existing `readNotifs()`); unread = full level-colored border + wash + dot, read = dim + chevron (never color-alone); header unread-count + Mark-all-read → green "All caught up" inert at zero; upgraded check-ring empty state w/ 3-item preview; bell `role=button` + aria unread count + 9+ badge cap; `.iconbtn` 42→44px floor (closes the T5 debt line). Per-row persistence/timestamps/dismiss store remain FOUNDER-GATED. Verify GREEN; unread/read/mixed/empty + click flow live-probed. Evidence: `t5-after-*.png`.
- 2026-07-12 — **plan-u1 · Shared composer() a11y** (same branch, commit `f571727`, tag `fable5/2026-07-12-sweep-plan-u1-composer-a11y`; NOT merged): notes/meal/coach×2 composers now render from one `components.composer()` — native aria-labeled `<button>` (Enter/Space, 48px, focus ring) + labeled input; foodsearch's handler-less send stays an `aria-hidden` decoration (never a dead button). Live-verified all four fire on Enter+Space at 390×844.
- 2026-07-12 — **meal-thread · Honest thread-failure + retry** (same branch, commit `e447aa3`, tag `fable5/2026-07-12-sweep-meal-thread-failure`; NOT merged): `fetchMealComments` returns the `{error:true}` sentinel; athlete thread gets in-place failure + 48px Retry (threadBusy-guarded); coach view gains a refetch-loop guard + same failure state instead of crashing on the sentinel. Verify GREEN; offline→retry→recover live-QA'd on `#meal-detail` + `#coach-meal`.
- 2026-07-12 — **persona-sim · Live persona simulation, 4 findings fixed** (same branch, commit `0a66a01`; NOT merged): full multi-persona walk (coach/athletes/trainer/parent/guest, evidence `.fable5/shots/persona-sim/`). Fixed: F-N1 HIGH `coach_set_goals` silent no-op (UPDATE matched 0 rows for profile-less athletes) → upsert in **authored-only migration `0054_coach_set_goals_upsert.sql`** (NOT applied; founder applies after `test:rls` — outside proto scope, flagged in run report); F-C2 revoked-consent distinct state + mount status-capture fix; F-C1 Copilot honest roster-offline card; B2 dead deep-link "Athlete not found" guard. Verified live on the test project; 1745 jest green.
- 2026-07-12 — **log-water · In-place tap patching** (same branch, commit `37a8710`, tag `fable5/2026-07-12-sweep-log-water-reanimation`; NOT merged): +8/+16 taps self-wire via `mount()` and patch the counter in place — no more 320ms sheetUp replay/scroll reset per tap; goal-cross still re-renders honestly. Water math untouched.
- 2026-07-12 — **breakdown-streak · Time-aware weigh-in copy** (same branch, commit `a531092`, tag `fable5/2026-07-12-sweep-breakdown-timeaware-copy`; NOT merged): `S.weightLine` no longer claims "streak reset" before the 9:00 AM window closes — splits on `minutesNow() <= WEIGHT_DUE` (reused requirements.js window): actionable "Weigh in by 9:00 AM…" before, byte-identical reset copy after. Verify GREEN; 3 states + inclusive boundary live-verified.

- 2026-07-22 — **Monthly Report locked state: honest actionable upsell** (branch `fable5/2026-07-22-onstandard-launch-ready-audit`; NOT merged): `upgradeCard()` was a dead end (lock icon + "Upgrade to unlock" with no button/route) that discarded the real month stats `load()` already computed. Rebuilt into `lockedCard(payload, period)`: real `.bigstat` average score + `.base-stats` (best/worst day, weight change, best streak) — same numbers `reportBody` shows once unlocked, factored into shared `baseStatsBlock`/`weightChangeStr` helpers so the two bodies can't drift — plus a frosted "Coach's take" veil (new `.mr-locked`/`.mr-skel`/`.mr-veil` CSS, theme-aware, degrades to a near-opaque scrim without `backdrop-filter`) that locks only the genuinely-premium written narrative, a green "Start free trial" CTA (`#mr-trial`, intent-only — fires `TRIAL_STARTED`, no billing/nav), and a blue "Have a sponsor code?" row routing to the already-registered `redeem-code` screen (the one path that unlocks premium today). `PAYWALL_VIEWED` fires once from `load()`'s post-fetch path (guarded by `CACHE.paywallFired`, not from `mount()` which runs before the async fetch resolves). Empty/error months degrade to `—` placeholders like `reportBody` always has; the "Your month, already counted" pill is gated on `loggedDays>0` (caught in QA — was showing on a genuine 0-day month). Progress's monthly-report teaser row (progress.js:139) gets a blue "Premium" status-pill so it reads honest before the tap. Verify GREEN (lint:xss clean, typecheck clean, 2389/2389 jest — one pre-existing unrelated suite `src/proto/bridge.test.ts` fails to parse `expo-file-system/legacy` ESM, confirmed present before this change too); proto.zip + protoVersion.ts rebuilt. Live-rendered via a headless-Chromium harness (real `monthly.js`/`components.js`/`icons.js`/`analytics.js`, stubbed only `state.js`/`roles.js`) at 390×844 both themes: real stats shown, only narrative frosted, both CTAs present, `redeem-code` data-go verified + 68px row height, `PAYWALL_VIEWED` fired exactly once across 2 paints, `TRIAL_STARTED` fired on tap, empty-month `—` degradation confirmed. Screenshots `.fable5/shots/2026-07-22-monthly-report-locked/`.

## Decisions (with rationale)
- Fable 5 never merges to master; the founder integrates. Rationale: LLM output is reviewed before it ships.
- 2026-07-11 — Trainer identity is server-hydrated into RT (never `RT.ob` scratch, never hardcoded personas like "Tracy Boone"/"Coach Mark"). Rationale: profile must survive reinstall/new device and never show another persona. SETTLED — do not re-litigate.
- 2026-07-11 — Tab-root role dashboards get chevron-less headers (`titleHead`), not `backHead(...,'profile')`. Rationale: back on a root routed cross-role into the athlete profile. SETTLED.
- 2026-07-11 — QR generation is a from-scratch, dependency-free encoder (no CDN in the WebView, no npm dep). SETTLED.
- 2026-07-11 — Practice HQ visual lane: trainer purple as accent inside the existing dark redesign system; Athlete Blue remains the athlete spine. SETTLED (design taste, founder may override).
- 2026-07-11 — Streak urgency is grace-calibrated, never flat: strong loss-aversion copy ("ends tonight") ONLY when grace is genuinely spent (`graceUsedRecently`); mild "covered" framing otherwise. Honesty over engagement pressure. SETTLED.
- 2026-07-11 — Home ribbons/prompts render as siblings of `strip()`, never children — strip owns `data-go="score-breakdown"` and nested data-go targets must not compete. SETTLED (pattern for future Home surfaces).
- 2026-07-12 — Router wires [data-go]/[data-act] ONCE at render; mount()-injected elements MUST self-wire clicks (bug found 3×). SETTLED.
- 2026-07-12 — supabase-js resolves network failures into `{error}` WITHOUT throwing — offline detection must use the `{error:true}` sentinel from the fetch layer, never a catch around the loader. SETTLED (fetchMyTeams/fetchMyPractices now follow fetchMyPracticeIdentity).
- 2026-07-12 — Never render a control that persists nothing (data-toggle-group theater): make it honestly inert + labeled preview, or remove it. SETTLED (checkin, coachVoice, quiet-hours).
- 2026-07-12 — CSS scoping traps: `.wb2` requires `.water-btns` ancestor, `.chp` requires `.chip-row` — bare use silently unstyles. Check the selector, not just the class name.
- 2026-07-12 — FOUNDER APPROVED (worklist checkpoint): T3 = Variant A dial-first WITH honest red at 0 ("Off Standard" keeps the muted-red pill); T5 = zero-new-data slice only (per-row read persistence / timestamps / dismiss store remain FOUNDER-GATED); T2 integrity rule = OPTION A — non-live (gallery) meals are NEVER scored and never count toward score/streak (explicit founder approval for this scoring-adjacent change; must be disclosed to the athlete BEFORE they commit a gallery pick); pass-2 sweep ranking + plan.js teardown approved as-is, plan.js A1 first. SETTLED.
- 2026-07-12 — Gallery meal photos are flagged `live:false` and disclosed with the amber NON-LIVE badge; scoring stays unchanged until the founder picks an enforcement rule (Option A block-scoring recommended) and authorizes any detection backend. Rationale: honest disclosure ships free; enforcement/detection is a product-integrity + migration/model decision. Spec: `reports/2026-07-12-t2-integrity-spec.md`. FOUNDER-GATED.
- 2026-07-12 — Notifications stays a routed full screen, never a drawer/panel: rows are actionable accountability moments, `#notifications` already participates in back-nav/deep-linking, and a drawer is a per-surface fork. (Design recommendation in T5; becomes SETTLED if the founder approves the build.)
- 2026-07-12 — Security migrations are authored on-branch, never applied to live (`0053_authz_hardening.sql` awaits founder `test:rls` + apply at go-live). Reaffirms the author-only guardrail.

- 2026-07-12 — T2 integrity Rule A is BUILT (non-live meals never scored, `110609f`); only the DETECTION backend (pHash migration / capture-token / model work) remains FOUNDER-GATED per `reports/2026-07-12-t2-integrity-spec.md`. Disclosure happens BEFORE the athlete commits a gallery pick (camera line + analyzing), honoring the approval condition. SETTLED.
- 2026-07-12 — Notifications stays a routed full screen, never a drawer: SETTLED (founder approved the T5 build on that design).
- 2026-07-12 — Read/unread must never be color-alone: unread = border+wash+dot, read = dim+chevron (weight + shape carry the state). SETTLED (T5 pattern for any future feed).
- 2026-07-12 — High-frequency tap counters (water +8/+16) self-wire in `mount()` and patch the DOM in place; the router's [data-act] auto-wire+re-render is for state changes that honestly change the screen. Rationale: re-render replays entrance animations and resets scroll on every tap. SETTLED (extends the 2026-07-12 router-wiring decision).
- 2026-07-12 — A send/submit glyph with no click handler renders as `aria-hidden` decoration, never a `<button>` — a focusable dead control is worse than none (foodsearch precedent in `components.composer()`). SETTLED.
- 2026-07-12 — Persona-driven live simulation (multi-role walk of real flows on the test project) is a legitimate QA lens: it caught a HIGH silent-save-failure (`coach_set_goals`) that unit tests and screen audits both missed. Keep `.fable5/shots/persona-sim/` as the recipe reference.

## Tech Debt
- Trainer identity needs two table reads (practices + profiles); an optional `practice_identity()` RPC would make it one round-trip — FOUNDER-GATED (requires migration; author-only, never apply).
- Practice HQ roadmap sections (business health, client health, AI assistant, analytics, default-standard mgmt, branding, integrations, business tools) exist only as LOCKED rows.
- Surfaces reading `e.now||e.overdue[0]` must handle the "everything time-locked" case (both null while score <80) — streakPrompt/notifications now fall back to score-breakdown (`2444ce5`); a shared `S.exec.nextActionable` helper is still worth it if a third surface appears.
- ~~2026-07-12 (T1 QA) `.vf-hint` 4.13:1 sub-AA contrast~~ FIXED 2026-07-12 in `156123a` (tag `fable5/2026-07-12-worklist-qa-followups`): `--text-3` → `--text-2`, browser-measured rgb(154,169,194) live.
- ~~2026-07-12 (T1 QA) `.vf-deadline` duplicates the capture prompt~~ FIXED 2026-07-12 in `156123a`: pill renders only when `!L.empty`; header keeps the real `L.due`; `L.remaining` data untouched. Prompt occurs exactly once (browser-probed).
- ~~2026-07-12 (T5 audit): header bell + back are 42px, 2px under the 44px floor~~ FIXED 2026-07-12 in `324bdc0` (T5 build): `.iconbtn` floor 42→44px, `.apphead .avatar` scoped to 44px; camera.js's inline 40px back-button override deliberately untouched (see new debt line).
- 2026-07-12 (T5 build note): camera.js still carries an inline 40px back-button override — under the 44px floor; fold into the next camera cycle.
- 2026-07-12 (T2A QA note, pre-existing): foodsearch "Enter Label" control measures 18px tall — sub-44px, predates the T2A diff; fold into the next foodsearch cycle.
- ~~2026-07-12 (T2A QA) non-live meal timing self-contradicted scoreStatus~~ FIXED 2026-07-12 in `89a7538`: `meal.js` `timing` now branches on `M.live===false` ("Logged late" / "Logged from gallery", no "still counts" claim) instead of reusing the live-capture string that collided with "Won't count toward your score" at the exec summary + thread header. Presentation-only; browser-verified all 4 late/on-time × live/non-live permutations at 390×844.

## Open Bugs
- ~~Parent tab root back-nav~~ FIXED in-tree (verified 2026-07-11 by screen-run audit + grep): parent root uses `titleHead('Parent view','Setting up access')` at coach.js:679. Memory was stale.
- ~~Offline trainer shown "minting"~~ FIXED in-tree (verified 2026-07-11): `fetchMyPracticeIdentity` returns `{error:true}` on fetch failure (roles.js:78) and `_loadPracticeIntoRt` sets `RT.practiceOffline` distinct from minting (state.js:489–505, surfaced at :902). Memory was stale.
- ~~Streak-run QA #1 (locked-window no-op CTA)~~ + ~~#2 (notif rows route:'home')~~ FIXED 2026-07-12 in `2444ce5`: shared root fix — `e.now || e.overdue[0] || score-breakdown` fallback with "View standard" CTA, notification rows compute the same route.
- ~~Streak-run QA #3 (green secured pill)~~ BUILT 2026-07-12 in `0485fea` — founder approved green over quiet.
- ~~2026-07-12 (T2 QA) `MEAL.live` leaks into manual entries~~ FIXED 2026-07-12 in `156123a`: `captureManual` now sets `MEAL.live = true` (manual entries have no photo provenance; presentation-only, no score impact).
- ~~2026-07-12 (sweep #1, plan.js A1)~~ FIXED 2026-07-12 in `94b3fc9`: `S.planTargets` returned `null` identically for coach-set-nothing / hydrating / offline and plan.js asserted "coach hasn't set targets yet" — an offline athlete with real targets was told their coach set none. `_loadProfileIntoRt` now destructures the SETTLED `{error}` sentinel and sets `RT.profileLoading`/`RT.profileOffline` (never clearing a cached target on a failed refetch); new `S.planTargetsState` getter (set > loading > offline > unset) drives plan.js. Live-verified all 5 states + retry at 390×844 (localStorage+mock-supabase Playwright harness, no console errors). Verify GREEN.

## Roadmap
- 2026-07-12 (founder-worklist run, all gates in `reports/2026-07-12-founder-worklist.md`): (1) T3 score-box BUILD pending founder approval of Variant A/B + red-at-zero call (prototype artifact 1d7ec88b…); (2) T5 notifications BUILD pending founder approval of Variant A/B + read-state/timestamp plumbing authorization (prototype artifact 61f0ebdb…); (3) T2 integrity ENFORCEMENT + detection gated on founder picking Option A/B and authorizing any pHash migration / capture-token / model work; (4) sweep PASS 2 pending founder calibration of the ranked list (`reports/2026-07-12-sweep-calibration.md`, plan.js first — offline false-negative A1 is the headline); (5) founder applies migration `0053` at go-live after `test:rls`; (6) merge decision on branch `fable5/2026-07-12-founder-worklist`.
- 2026-07-12: FULL COVERAGE COMPLETE — all 30 ledger rows done, 40+ improvements shipped, branch awaiting founder merge. Next: (0) founder merge decision on the branch; then pass 2 from the ranked backlog in `coverage.md` (plan offline false-negative copy, progress green-on-negative delta, camera vf-tools 40px, meal thread-load failure state, log sheet re-animation, home NEXT/LATER-vs-OVERDUE hierarchy); (a) first unlocked Practice HQ section — client health list is the highest-value candidate (real client rows already reachable via widened `fetchMyPractices`); (4) founder decision on the `practice_identity()` RPC proposal.
- Streak surface follow-on candidates (audit runners-up, still valid): billing-conversion polish (must respect the deliberately-unwired Stripe checkout seam) and coach-roster triage. Both fold to presentation-only changes.
- Coach profile likely shares the stale-scratch identity pattern trainer had (roles.js `coachProfile` read `RT.ob` too) — audit it before building coach-side HQ.

- 2026-07-12 (approved-builds + sweep1 run, report `reports/2026-07-12-approved-builds-sweep1.md`): worklist items T2A/T3/T5 are BUILT and the sweep pass-1 batch shipped (plan-a1, plan-u1, progress-delta, meal-thread, persona-sim, log-water, breakdown-streak) — all on `fable5/2026-07-12-founder-worklist`, tag `fable5/2026-07-12-approved-builds-sweep1`, awaiting founder merge decision. Next batch, in order:
  1. **Recovery teardown build** (seeded, `reports/2026-07-12-recovery-teardown.md`, all presentation-only, nothing gated): R1 form `hideTabs` (FAB/tab-bar collide with Submit — live-measured), A1/D1 coachless "Coach can see your update" caption, A2 recovery-confirm global `RT.lastMove` mis-attribution/false "+0 pts" celebration, A3 readiness read-out, U1 chip magnitude fill, U2 soreness polarity, U3 confirm green-on-purple color fork, U4/U5 coach-mention triplication + buried "+30" payoff, A4/A5 gain clamp honesty + completeness affordance, R2 disclaimer placement.
  2. **Remaining plan.js teardown items** (from the approved calibration): A2/A3, U2–U4, R1–R2, D1–D2.
  3. **Residuals**: breakdown (rest of the teardown beyond the weigh-in copy), camera (inline 40px back button; "Enter Label" 18px in foodsearch), notifications (anything beyond the zero-data slice).
  4. **FOUNDER-GATED, do not build without approval**: T5 read-state/timestamp/dismiss persistence plumbing; T2 detection backend (pHash migration / capture-token / model) per `reports/2026-07-12-t2-integrity-spec.md`.
  5. **Founder ops at go-live**: apply authored migrations `0053_authz_hardening.sql` + `0054_coach_set_goals_upsert.sql` after `test:rls`.

## Launch Checklist
- See docs/LAUNCH-CHECKLIST.md
