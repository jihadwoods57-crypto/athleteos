# Crew Report — 2026-07-10 (branch `crew/2026-07-10`)

**Run status:** interrupted by a session/rate limit (reset 12:20pm America/Chicago) partway
through cycle 2. The parallel **discovery** phase completed — 22 scouts, **58 findings**, 1.87M
subagent tokens — but the `prioritize`, implementer, and `final-report` agents were killed by the
limit. This report was reconstructed by the operator from the workflow journal, and **one provable
fix was prioritized, implemented, gated, and tagged by hand** to honor the trust model (tests decide
survival, founder decides merge). Nothing was merged to master.

## Shipped this run (gated, tagged, NOT merged)

### ✅ `crew/2026-07-10-i1` — Stop fabricating the parent Weekly-Compliance headline from seeded demo data
- **Pillar:** reliability / honesty · **Severity:** high · **Constitution:** *"demo data is never the
  default state" + "engagement is earned, never manufactured."*
- **Defect:** `trendSeries()` left-pads a sparse real history with the hardcoded
  `SEEDED_LEAD = [82,80,83,86,88,85]` (every value ≥ the 80 compliance bar). `weeklyCompliance()` and
  `nutritionTrend()` then computed their **headline numbers from that padded series**, so a *real*
  parent whose athlete has < 6 days of history saw e.g. **"5 of 6 days on plan · 84%"** and a row of
  green ✓ dots — entirely manufactured from demo padding. The behavior was **untested** (the only
  `weeklyCompliance([], …)` test asserted the today-marker, never the fabricated aggregate).
- **Fix:** aggregates now count **real completed days only** (leading seed excluded via
  `realTrendDays`). Seeded points are flagged (`ComplianceDay.seeded`, `NutritionTrend.seededBefore`)
  and `ParentView` renders them as neutral placeholders instead of green checks / filled bars; a
  no-history athlete shows an honest **"Building this week — no completed days yet"** and `—` instead
  of a made-up percent. Padded series kept for chart shape so dots still match the trend line.
- **Tests:** corrected 2 tests that encoded the fabrication; added 4 honesty tests. `npm run verify`
  green (tsc + jest + iOS bundle). Files: `src/core/history.ts`, `src/core/history.test.ts`,
  `src/screens/roles/ParentView.tsx`.
- **Revert:** `git reset --hard crew/2026-07-09-i1`.

> **Note (constitution "Measurement" rule):** this is a truthfulness correction, not an engagement
> lever, so no new analytics event was added — the proof is the test. Flagging for your call: if you
> want to *measure* how often the honest empty-state shows (i.e. how many parents are pre-history),
> that's a one-line `track('parent_week_empty')` I can add next cycle.

---

## Provable backlog — shippable next, ranked (impact × confidence ÷ risk)

These are code-gateable by the existing oracle and were **not** shipped only because the run stopped.

| # | Pillar | Sev | Finding | Where |
|---|--------|-----|---------|-------|
| 1 | reliability | med | **Same seeded-lead leak in the ATHLETE score delta** — Home "this week" delta is `series[last]-series[0]` where `series[0]` is a SEED value for days 2–6. Sibling of the fix just shipped. | `src/core/scoring.ts:375,385` |
| 2 | design/**data-loss** | high | **Duplicated "Sign out" button, one skips the confirm dialog** and erases local-only data on a device that never synced. Byte-identical copies drifted. | `Profile.tsx:447`, `Account.tsx:239` |
| 3 | ai | med | **Number-preservation guard splits decimals** — `/\d+/g` tokenizes `2.3`→`[2,3]`, so a rephrase can flip `2.3 → 3.2 meals/day` and still pass the guard. | `src/core/nutritionMemoryVoice.ts:30-33` |
| 4 | architecture | med | **Coach roster reads truncate nondeterministically** — `.limit(1000/2000)` with **no `.order()`**; PostgREST row order is unstable, so a large roster silently drops rows. Low-risk (add `.order`). | `src/lib/supabase/queries.ts:326-349` |
| 5 | reliability | med | **Trust-pass median polluted by camera-free 0-days** — rollover archives a 0 for every opened-unlogged day, so "median of last 10 **photo-earned** days" is really last 10 **calendar** days. Firewall-safe but off-spec. | `src/core/dayRollover.ts:74-78` |
| 6 | architecture | med | **Fresh sign-in overwrites granular role with coarse DB enum** — nutritionist/trainer subtype personalization silently degrades on a new device. (Constitution roles rule.) | `src/store/useStore.ts:1187-1189` |
| 7 | reliability/perf | med | **Meal-history thumbnails sign photo URLs one-by-one (N+1)** — one storage round-trip per card. | `src/screens/overlays/MealCardItem.tsx:22` |
| 8 | design/a11y | high | **Success-green text fails WCAG AA (2.28:1)** on white cards, incl. the active tab label — needs the `test:design` gate built to fence it. | `AthleteApp.tsx` + palette |
| 9 | design/arch | med | **No shared trend-chart primitive** — the SVG line+area render is copy-pasted in **6+ screens** and has already drifted. | 6 screens |

**Infra the crew is meant to build (high leverage, currently advisory gates):**
- **`test:ai`** — no eval harness exists; meal-analysis accuracy / hallucination / scoring drift are
  completely unguarded. (`package.json`, oracle `builtOracles.ai = not-yet-built`.)
- **`test:design`** — no a11y/contrast gate; the only guard covers 4 of the palette's pairs.

---

## Founder judgment backlog — NEVER auto-shipped (your call)

The scouts surfaced a striking pattern: **several complete, tested features are built but never
rendered.** These are the highest-leverage product moves and cost little to wire up.

**Orphaned / dead high-value features (built + tested, zero UI entry point):**
- 🔴 **Comeback re-entry card** — a complete, 7-test module built for *"the #1 way accountability apps
  lose people"* (athlete misses days, feels behind, quits) is **never rendered**. `src/core/comeback.ts`.
- 🔴 **Discipline Record** — the recruiting share card + Individual-Plus seller (4 tests) was
  **orphaned by the 2026-07-07 Profile redesign**. `src/core/disciplineRecord.ts`.
- 🔴 **Deep Dive** — the AI weekly-analysis paid lever is fully wired to the backend with a plan gate
  and weekly cap, but has **no UI caller anywhere**. `src/lib/ai/deepDive.ts`.
- **Performance (~384 lines) + Nutrition (~632 lines)** athlete screens exist but are **unreachable**.

**Core promise gaps:**
- 🔴 **"Message" — the single largest CTA on every athlete — leads to a dead channel.** The
  "replace texting your athletes" promise is non-functional when live. `Messages.tsx`, `PersonDetail.tsx:329`.
- 🔴 **Invite deep-link flow is dead code** — every share sends **plain text, never the deep link**;
  the referral case (invitee doesn't have the app) **silently drops the code**. One-tap join never
  fires. `CoachView.tsx:76`, `inviteLink.ts:26`.
- **Paying parent's home is a passive dead-end** — no resend-link, no sign of whether the athlete
  joined; and the intervene / no-intervene verdict (the one thing they pay for) is the **6th card,
  below the fold**, and when it flags "check in" it never says **about what**. `ParentView.tsx`, `parent.ts`.

**Real bugs worth confirming (may be provable next cycle):**
- **Tapping "Log breakfast" / a specific meal row opens capture with the WRONG meal preselected** —
  `openMeal` ignores the slot the CTA computed. `useStore.ts` / `nextAction.ts`.
- **Progress Week/Month/Season toggle is non-functional** — taps change the label, not the data
  (`Segmented` has no `onChange`). `Progress.tsx:180`.

**Redundancy / simplify (design taste — proposals, not auto-shipped):**
- Home's "Finish Today" card is a **third copy** of the hero + requirements already on screen (same
  CTA twice, progress three ways). Also flagged independently by the design critic. `Home.tsx`.
- Nutrition shows protein progress **three times**; Progress ends with **three prose cards** stating
  the same insight; Profile lists the accountability circle **2.5×**.
- Motion asymmetry (overlays animate in, vanish on close), silent athlete nav (no haptics vs coach
  tab bar that has them), 4 simultaneous accent hues on Home's requirement list.

**Security / migration (needs a DB migration you apply — already in `backlog.md` P1–P2):**
- `base_age` athlete-editable → minor bypasses parental-consent + supervision gates. **COPPA-adjacent.**
- `analyze-meal` finalize/memory/order modes **bypass the global AI-spend ceiling** via the public
  anon key → unmetered paid Sonnet-vision spend. Constitution: *"AI spend caps stay in force."*
  Fix is code-authorable but touches the edge function (crew never deploys) → **founder-gated**.

---

## Why the run stopped & how to finish it
The workflow hit the account session limit (resets **12:20pm America/Chicago**). The discovery
tokens are already spent and cached in the journal, so re-running is cheap. **To continue after the
reset:** just say *"continue the crew"* — I'll prioritize and ship the next provable item (the
`scoring.ts` seeded-delta sibling, #1 above, is the natural next cycle) under the same gate.

## Founder actions
1. Eyeball the `crew/2026-07-10-i1` diff (3 files) and, if good, merge/cherry-pick to master — the crew won't.
2. Pick from the judgment backlog: the **orphaned features** are the highest impact-to-effort ratio in the app.
3. Decide the two migration items (`base_age`, AI-spend ceiling) — they need your `supabase` apply.
