# 2026-09-15

## 1 PM Audit

Report of record per the standing path.

- **Shipped:** one fix batch, live-ready (`ecdd984`). The big one: pressing Enter
  mid-word on a Japanese or Chinese keyboard could fire off half-typed text on
  nineteen different inputs — sign-in, password change, coach notes, renames, the
  meal question sheet — including one added just this morning. All of them are guarded
  now, and a new automated gate makes sure no future screen ships without the guard
  (the gate was proven to fail on yesterday's code and pass on today's). Three smaller
  fixes riding with it: the score trend chart now draws its "scoring changed here"
  divider for last week's scoring change (it silently skipped it all week); the AI's
  blue highlight line in chat got its extra weight back after this morning's bolder
  thread text flattened it; and long participant lists on a meal thread now trail off
  with "…" instead of chopping a name mid-letter. Proof: all 16 gates green, a
  155-screen sweep with zero crashes and zero errors, and the rebuilt bundle changed
  exactly the 17 files touched — nothing rides along. **Ready but not shipped** — the
  publish token is still dead (checked once this morning, per the standing rule), so
  one publish from the PC makes it live.
- **Found (and cleared):** the six-day-old debt on the scoring rewrite is PAID. I
  attacked the new score math the way the 09-09 backlog demanded: a perfect eating day
  crosses 80 with no check-in on every profile and plan style, the four-protein-shakes
  day reads 70 instead of the old dishonest 100, answering the check-in honestly with
  low numbers costs nothing versus tapping all 9s, a late meal fades instead of
  cliff-dropping, 100 is reachable, and frozen history has boundary tests on both sides
  of both scoring eras. The engine held everywhere; the only v3 miss was that chart
  divider, now fixed. Also checked this morning's bolder-chat ship: honest commit, zip
  matched, all four thread renderers consistent — its one miss was the flattened
  highlight above.
- **Didn't do:** no publish (dead token, streak continues); no "before" screenshots
  (the fixes are one-line CSS/guard changes — the after-shots are committed in
  `.crew/reports/2026-09-15-shots/`); left the known-accepted sweep flags alone
  (composer pill heights, horizontal scroller "wide" flags) per the backlog ledger;
  no browser preview (no Cloudflare token — skipped silently per charter).
- **Recommend:** publish from the PC — the IME guard batch protects every signup and
  sign-in field, and it is sitting ready. Then the standing ask: working cloud
  credentials would have made today's fixes live by now.

## Error response (mid-day)

Report of record per the standing path.

- **Why this session fired:** the error monitor flagged a spike (or this was the
  scheduled no-op check — can't tell which from here). Still no Supabase credentials in
  the cloud env (streak continues), so the actual error events are invisible: no query
  for what error, which screen, or how many users. But this firing was NOT a repeat of
  this morning's blind proof — four new founder commits (`54cf111` → `507ddea`, pushed
  from his PC between 00:10 and 04:50 UTC) landed after the last sentry proved the tree,
  and re-proving new commits is the job.
- **The key fact:** the founder also PUBLISHED from his PC — the live OTA on both iOS
  and Android is byte-for-byte his latest zip (md5 `9920dc76…` + sha256 match at
  `507ddea`). Real users are running the new coach/profile/chat/roll-call work right now.
- **What I proved on that live tree:** all 16 verify gates green; 155-route headless
  sweep with zero crashes and zero JS errors; the two live edge functions the new code
  leans on (`athlete-summary`, `meal-chat`) probed with the app's own public anon key —
  both deployed and answering, and `athlete-summary` correctly rejects an anonymous
  caller with 401 even with a valid athleteId in the body, so the `verify_jwt = false`
  pin (his 04:50 commit) is live AND the function's own auth doors hold: caller
  identity, `can_view`, and roster membership are all checked in code.
- **Verdict:** no reproducible error in the newly shipped code. If the monitor's spike
  was real it happened server-side or on-device where I can't see without DB eyes — but
  the plausible reading is that the founder's own 04:50 `verify_jwt` fix WAS the
  response to whatever spiked (a function rejecting calls at the gateway would look
  exactly like an error spike), and it is deployed and working. Nothing shipped because
  nothing was found broken; no incident email because no incident was confirmed.
- **Found (for the audit session):** sweep flags grew slightly with the new screens —
  17 small tap targets (was 13) and 6 clipped-text cases (was 1). The clipped-text jump
  is worth eyes: likely the new profile/coach screens.
- **Didn't do:** no fix, no zip rebuild, no publish (nothing broken; and the founder is
  clearly publishing from his PC anyway). Did not re-check EXPO_TOKEN — this morning's
  session already spent today's one re-check (still dead).
- **Recommend:** same standing ask — read-only Supabase credentials would turn these
  blind firings into one query. Second recommendation: when the founder ships a fix to a
  live incident from his PC (like the 04:50 verify_jwt pin), a one-line "OS:" email
  telling the sentries what spiked would save every subsequent session the forensics.

## Error response (early hours)

Report of record per the standing path.

- **Why this session fired:** the error monitor flagged a spike in live error events (or
  this was the scheduled no-op check — from here I can't tell which). Still no Supabase
  credentials in the cloud env, so the error events themselves are invisible to me: I
  cannot say what error, which screen, or how many users. What I *can* do while blind is
  prove what users are running and attack it, so that's what I did.
- **What I proved:** the live OTA on both iOS and Android is byte-for-byte the current
  tip of master (md5 + sha256 match against `assets/proto.zip` at commit `33a3311`,
  last night's group-chat ship). All 16 verify gates are green on that exact tree. Then
  I swept every registered route headlessly — all 154 captures rendered, **zero
  crashes, zero JS errors**. Six days of new commits (09-11 through 09-14: the roll-call
  alarm work, the meal receipt and correction fixes, the group-chat restyle) are live
  and none of them breaks a screen at open time.
- **Verdict:** no reproducible error found. If the monitor's spike was real, it is not a
  click-time crash on any registered route — which leaves server-side (edge functions),
  device-only paths (camera, HealthKit), or a false alarm, and none of those are
  checkable without database eyes. Nothing shipped because nothing was found broken;
  no incident email because no incident was confirmed.
- **Found (cosmetic, for the next audit session):** the sweep's defect report flags 13
  small tap targets and 1 clipped-text case across the app. No crashes, no dishonest
  states surfaced by the machine sweep.
- **Didn't do:** no fix, no zip rebuild, no publish (nothing to fix; publish token is
  dead anyway — re-checked once per the charter, still "bearer token is invalid").
- **Recommend:** the standing ask, again, now with an error-response session as the
  proof of cost: read-only Supabase credentials would have turned this whole session
  into one query. Until then, every error-response firing can only prove the client
  code green and shrug at the actual numbers.
