# Crew Cycle Report — 2026-07-09 · i1

**Hand-run** of cycle 1. The autonomous Workflow couldn't launch from the operator's PlaySmith-rooted
session, so the same discipline (discover → prioritize → implement → gate → tag) was orchestrated by hand
against OnStandard. Baseline confirmed green (124 suites / 1555 tests / iOS bundle) before any change.

## Outcome
5 provable fixes shipped and gated; 2 security issues + several judgment items routed to the founder
backlog (never auto-shipped). **Single highest-impact change to make next: proposal P1 (base_age consent
bypass)** — it needs a migration, so it is yours to apply.

## Shipped — 5 fixes, oracle-gated, tag `crew/2026-07-09-i1`
Gate after the batch: `npm run verify` green — tsc clean, **125 suites / 1563 tests** (+8 new), iOS bundle OK.

1. **[reliability] Home "vs yesterday" → "vs last week"** — Home labelled a full-week score delta as
   day-over-day; ParentView already labelled the same value correctly. `src/screens/athlete/Home.tsx`.
2. **[reliability] Progress: honest building-state until a real week** — the weekly card counted seeded
   showcase days (all ≥80) as real for the first ~6 days of every athlete ("4 of 6 on standard" from two
   bad days). Now requires a full real week before the card shows. `src/screens/athlete/Progress.tsx`.
3. **[reliability] App error boundary** — none existed; one render throw blanked the whole app. Added
   `src/ui/ErrorBoundary.tsx` (honest fallback + retry) and wrapped `src/Root.tsx`.
4. **[reliability] Weekly recovery carry survives the round-trip** — the day save stored only `ciLast`'s
   date, so on a 2nd device the carry collapsed and the same lived day re-scored 25–35 low, then overwrote
   the server row. Persist the magnitude as a sibling key `ciLastRecovery`; restore both.
   `src/store/sync.ts` (+2 round-trip tests). `ciLast` stays a plain date string for the 0041 SQL trigger.
5. **[reliability → constitution] Lose-fat client scored on gain defaults** — a managed client's coach-set
   plan (targets + scoring profile) was read only by the coach's editor, never by the client on sign-in,
   so a fresh sign-in fell back to the gain defaults (+13 lb, 3200 kcal, 180 g) — tripping "never tell a
   lose-fat athlete to gain". Added `hydrateAthletePlan()` (RLS confirmed: `can_view` includes `is_self`),
   wired into both sign-in paths. `src/store/useStore.ts` (+6 tests).

## Rejected / reverted
None — all 5 passed verify + the constitution. Nothing auto-reverted.

## Founder-gated proposals & judgment items
See `.crew/backlog.md`. Top two need a **database migration** (crew never applies live schema):
- **P1 — `base_age` consent bypass** (security, high): a minor can set age 18 and bypass parental consent + supervision.
- **P2 — `days.score` fabrication ceiling** (security, medium): fabricate a ≥80 with no photo, game trust-pass.

## Verify items (for a follow-up cycle)
- Fix #2 hydrates targets + scoring profile; `baseGoal` (gain/lose onboarding field) is left as-is —
  confirm the goal-direction tone fully tracks the coach plan without also setting `baseGoal`.
- Add a property-based `mapStateToDayRow ↔ dayRowToState` round-trip test (ciLast was the 2nd field to slip).
