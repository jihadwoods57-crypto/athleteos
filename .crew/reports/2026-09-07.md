# 2026-09-07

**[2026-09-07 — Error response (overnight)]** — report of record per the standing path.

- **The spike question:** unanswerable — no Supabase creds in the cloud env, so the
  error/analytics events are invisible. Credential streak now **16 sessions** (EXPO_TOKEN
  re-checked once: still "bearer token is invalid"). No fresh diagnosis, per charter.
- **What I could prove, I did:** two commits landed from the founder's PC last night at
  ~8:30 PM ET, after the 7 PM session (47beb85 folds #devices / #verified-discipline /
  #progress-compare into their owning screens; d4e8395 fixes the tap-target sweep).
  Neither had been session-proven, so I ran the gates on the new tree: **all 14 green**
  (verify runs 14 now — trust its count). No click-time regression the gates can catch.
- **What's actually live:** the OTA manifest still serves the 09-03 zip (commit 4ca1808,
  md5 d64faefd…). Four days of proven work — including last night's screen folds — is
  stacked unpublished behind the dead token. If real users ARE hitting an error tonight,
  it's in the 09-03 code, and I can neither see it nor ship past it.
- **Didn't do:** no fix (nothing visible to fix), no email (no new question — the 08-26
  `OS?` key request still stands and covers this), no screenshots, no re-sweep of the
  unchanged parts of the tree.
- **Recommend:** unchanged and now more urgent — one credentialed sitting (fresh
  EXPO_TOKEN + Supabase creds) publishes four days of proven work and gives the
  overnight sentries back their eyes.

**[2026-09-07 — Error response]** — report of record per the standing path.

- **The spike question:** unanswerable, same as every session this streak — no Supabase
  creds in the cloud env, so if real users are hitting an error right now I cannot see
  it, count it, or trace it. Credential streak now **17 sessions** (EXPO_TOKEN's one
  permitted re-check: still "bearer token is invalid"). No fresh diagnosis, per charter.
- **What I could prove, I did:** two meal-screen commits (a35232c the clarifying-question
  sheet, d90377a the Back to Home footing) landed after the last sentry's proof, so I
  re-proved the tree: **all 14 gates green**. Both commits carried their rebuilt zip and
  protoVersion stamp — nothing stranded. (First verify run showed 4 red gates; that was
  the fresh sandbox missing `npm install`, exactly as the backlog note warns, not real
  breakage.)
- **What's actually live:** still the 09-03 zip. Any error a real user hits tonight is in
  four-day-old code that every session since has been unable to publish past the dead
  token.
- **Didn't do:** no fix (nothing visible to fix), no email (the 08-26 `OS?` key request
  still stands and covers this), no re-sweep of already-proven surfaces.
- **Inbox:** checked first, per charter — no unread `OS:`/`OS?` messages from the founder.
- **Recommend:** same single unblock — one credentialed sitting restores publishing and
  the sentries' eyes.

**[2026-09-07 — 8 AM Build]** — report of record per the standing path.

- **Pulse:** blind, same streak — no Supabase creds, no Stripe key (18 sessions through
  this one; the backlog note is updated). Inbox checked first: no unread `OS:`/`OS?`
  mail from you. The overnight sentries already re-proved your weekend meal commits.
- **Shipped (ready, not live):** every on/off switch in the app is now the whole row,
  not just the little pill. Before today, tapping the words "Team standard reminders" or
  "Show my number to the squad" did nothing — you had to hit the 50x30 pill itself. Now
  the full row toggles, the way iPhone settings work. This landed on eleven switches:
  Face ID, all four notification switches, all four coach notification switches, the
  squad share-my-score row, and the three signup consent rows (plus the coach signup's
  four notification rows). Screen readers now also hear the explanation under each
  switch (the squad row's "never meals, photos, or check-ins" promise was silently
  unreadable to them mid-build — the adversarial review caught it, and it's fixed).
  A new test (switch-rows, 5 checks) pins the pattern so no new screen can paste the
  old one back. All 14 gates green, twice (before and after the review fixes). The
  proto zip is rebuilt and stamped; entry-by-entry proof against the old zip: 0 files
  added, 0 removed, exactly the 4 source files touched. **Ready but not shipped — no
  publish credentials** (the token in this sandbox still answers "bearer token is
  invalid"; that was today's one permitted check).
- **Found:** the coach standards editor's own switch rows (the original pattern) carry
  a label that overrides their subtitle for screen readers too — same issue I fixed
  elsewhere today, pre-existing there. Cheap fix; left for the polish session so this
  build stayed one thing done fully.
- **Didn't do:** no before/after screenshot pair — the change is invisible in a still
  (same pill, bigger target), so I committed the two clean after-shots to
  `.crew/reports/2026-09-07-shots/` per the 09-06 precedent instead of hand-shuttling
  images through the Drive connector. The machine sweep on both screens: 0 errors,
  0 small targets, 0 clipped text.
- **Recommend:** the 1 PM audit owes backlog #0 first (your unaudited audience-picker
  commit), then should walk today's switch rows with a keyboard.
