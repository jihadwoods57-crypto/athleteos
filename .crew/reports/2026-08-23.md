# 2026-08-23 — 1 PM Audit + Fix

(Belongs at the top of the Daily Ops doc. The Drive connector still can't edit an existing
Doc's content — re-tested this session, its update tool only renames and moves files — so per
the standing rule this lives in the repo.)

- **Shipped (ready, OTA still blocked on the token):** three real fixes to the coach/trainer
  side, all gates green (12/12; the SQL suite didn't run, this sandbox has no Docker), zip
  rebuilt and committed. Users won't get them until the Expo token is replaced (still dead —
  I re-tested it, Expo rejects it outright), same wall the 8 AM session hit.
  1. **The coach "Activity standard" board and its manage list were broken on direct entry.**
     If a coach opened the app straight to one of those screens (a relaunch that restores the
     screen, or a tapped link) before anything had loaded their team, the board sat on
     "Loading the board…" forever, and — worse — the manage list flat-out said "No activity
     standards yet" to a coach who has three. That reads as "the app deleted my standards."
     Now both load their data on open and show the honest thing: the real board, a plain
     "nothing scheduled today," or a "can't reach your team — try again" with a working retry.
  2. **The "Rooms" screen had the same disease** (stuck skeleton on direct entry); same fix.
  3. **Two small honesty/polish nits the automated screen sweep caught:** the "open Privacy &
     visibility" link on the Delete Account screen was a too-small tap target (16px tall) and
     didn't look tappable — fixed both; and I removed an em dash from a coach error message to
     hold the no-em-dash rule.
- **Found:** (a) The visual-proof screenshot tool has never actually run in the cloud — it
  looked for Chrome in the wrong place and refused to start as root. I fixed it, so this is
  the FIRST session with real screenshots and the automated defect sweep. The sweep is how I
  found the coach-board and rooms hangs. (b) The root cause under both hangs is a fragile
  pattern: the coach data loader only repaints a hard-coded list of screens when the team
  finishes loading, and three screens had fallen off that list. I patched the list and filed
  the systemic fix (make screens subscribe instead of hard-coding) as backlog #8.
- **Proof:** all 12 gates green on a fresh checkout; zip rebuilt and compared entry-by-entry
  against the last commit — exactly the 5 files I touched changed, 0 added, 0 removed (nothing
  rode along). An adversarial review agent found 5 real ways my first cut was still wrong
  (the failure branch still hung, the retry couldn't actually retry, a held board could show
  dead buttons, a trainer saw "team" instead of "practice"); I fixed all 5 and re-verified.
  Before/after screenshots captured; the fixed coach-board is uploaded to the "OnStandard
  Screenshots" Drive folder: https://drive.google.com/file/d/1cPgxsDOoNiPTe8oM5HhOe8nBySpiUuf9/view
  (the rooms shot is captured too but the Drive image upload from this session is a
  base64-relay that's flaky above a few KB — worth a proper path someday).
- **Didn't do:** no DB/money/complaints pulse — this sandbox still has no Supabase or Stripe
  credentials. Left the bigger backlog security items (#2, #3) which need live-DB access.
  Didn't flip any feature flag; these were fixes to existing broken surfaces, not new features.
  Inbox checked: no unread OS directives from the founder.
- **Recommend:** still the token. Mint a fresh Expo access token and replace EXPO_TOKEN in the
  cloud env — that ships today's coach fixes plus the three the 8 AM session already staged.
  It's the one thing standing between finished work and real users' phones.

---

# 2026-08-23 — Sunday META · Week in review

(This entry belongs at the top of the Daily Ops doc. It's here instead because the
connected Drive tools can read and create files but cannot edit an existing Doc's
content — first real capability gap the system hit. Flagged for your ruling in the
backlog; until then the repo is the report.)

## The week's grade: A- on the work, incomplete on the last mile

This was the setup week — the charter went in Saturday night and the first daily
sessions run today — so there are no daily sessions to grade yet. What there IS is a
week of shipping (about 37 commits, Aug 16–22), and it holds up well:

**High-value, real:** the health-connect screen stopped claiming a connection Apple
never confirms (that was a straight-up lie to any athlete who unchecked every box);
HealthKit got wired with the right module after the docs named one that could never
work; keyboard users can finally reach every knob the stylesheet always promised a
focus ring for; the day screen stopped hauling two months of meal data to paint today;
and the trust screen now shows the exact photo-day count the server checks, not a
client guess.

**The standout:** the adversarial-review rule earned its keep in its first real outing.
The review of the day-load split caught that the trust screen's "N of M photo days"
would have RUN BACKWARDS for long-tenured athletes the moment the window narrowed.
That's the exact class of quiet regression the rule exists for, and it was fixed before
the push, with a regression net. Keep that rule sacred.

**What was NOT proven:** nothing in the week's log claims an EAS publish or a live
manifest hash match. Zips are rebuilt and committed, gates are green — but by our own
rule 4, until the live manifest matches, users may still be running code from before
all of it. That's now backlog #1 for the first session with publish credentials.

**What the founder had to correct:** the charter itself, twice within an hour of
seeding — the error-watch design assumed webhooks the cloud can't mint. Today added a
third instance of the same disease: the reporting section assumed the Drive connector
can edit the ops doc, and it can't. Pattern named and now a standing gotcha: prove a
tool exists in-session before writing it into the charter.

**Staleness caught:** AGENTS.md said verify runs "nine gates"; it runs twelve. That's
the same file that once said v56 for the whole SDK 57 cycle. Fixed, and worded so the
next staleness is self-evident.

## What the charter now does differently

- Sunday META is a named role in the schedule, not just a constitution footnote.
- Six new gotchas from this week's burns: server-enforced numbers must come from server
  rows, not client proxies; optional `require` of native modules is load-bearing for
  OTA; prose docs lie — trust package.json and verify's own summary; entry-by-entry zip
  compare is the proof-of-scope standard; failed reads keep the last known value; and
  charter mechanisms must be proven in-session before they're written down.
- Reporting: the `.crew/reports/` fallback is now the acknowledged standing path until
  a Docs editor exists — no second doc, no copies.
- Backlog: old #4 marked done (it shipped Saturday); new #1 is proving the OTA actually
  carries the week's committed work; the em-dash count flagged stale (246 already
  removed); a fourth "awaits founder ruling" item for the doc-write gap.

## Frozen sections

Red lines and the numbered Shipping discipline rules: untouched, verbatim. No change
proposals this week — they all held up in practice; rule 6 (adversarial review) paid
for itself and rule 4 (prove it) is what exposed the publish gap.

## Recommend

Rule on the doc-write gap (backlog, "Awaits founder ruling"): either wire a Google Docs
editor into the connector setup, or bless the repo reports + daily digest email as the
permanent surface. Everything else the sessions can carry from here.

---

# 2026-08-23 — Error response (night sentry)

(Belongs at the top of the Daily Ops doc; the Drive connector still can't edit an
existing Doc — re-tested tonight, its update tool only renames/moves files — so per the
standing rule this lives here.)

- **Shipped:** nothing. Nothing to fix that I could see.
- **Found:** the sentry is blind. This cloud environment has no Supabase credentials
  (only `EXPO_TOKEN` is set — no `SUPABASE_ACCESS_TOKEN`, no `SUPABASE_DB_PASSWORD`),
  the anon key isn't in the repo (it lives on EAS env), so there is NO way to read the
  live error/analytics events from here. I cannot say whether tonight was quiet or on
  fire — only that I can't see. That's worse than a quiet night and worth fixing: until
  Supabase credentials are added to this environment, both nightly sentry runs are
  no-ops every night.
- **Sanity checks that did run:** master is clean at cd18eb5 (today's Sunday META
  commit), no divergence from origin; no unread founder directives (OS:/OS?) in the
  inbox.
- **Didn't do:** no email — the charter reserves that for questions and the digest, and
  there's no user-facing incident to report, just a tooling gap.
- **Recommend:** add `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD` to the cloud
  environment's env vars so the sentry (and the 8 AM pulse) can actually see the DB.

---

# 2026-08-23 — Error response (second night sentry, ~5:30 AM)

(Belongs at the top of the Daily Ops doc; Drive connector still metadata-only — re-tested.)

- **Shipped:** nothing — nothing changed and nothing visible to fix.
- **Found:** same blindness the 3:38 AM sentry reported: no Supabase credentials in this
  environment, so live error/analytics events are unreadable. No spike can be confirmed
  or ruled out. Master unchanged since that sentry's own report commit; inbox has no
  unread OS:/OS? directives.
- **Didn't do:** skipped a full `npm run verify` — the code on master is byte-identical
  to what yesterday's sessions already proved green, so re-running the gates on the same
  tree tells us nothing about live users.
- **Recommend:** unchanged and now twice-reported — add `SUPABASE_ACCESS_TOKEN` +
  `SUPABASE_DB_PASSWORD` to the cloud environment so the sentry can see. Until then
  every overnight run is a no-op.

---

# 2026-08-23 — 8 AM Build

(Belongs at the top of the Daily Ops doc; re-tested the Drive connector this morning and
its update tool still only renames and moves files, so per the standing rule this lives here.)

- **Shipped:** two things, one small and one that isn't code. First, the verify suite now
  passes on a fresh checkout: the typecheck gate was failing in every cloud session because
  the commercial-video side project (scripts/commercial/remotion) has its own dependencies
  that a fresh clone never installs. That folder has its own TypeScript config, so the main
  project's typecheck now leaves it alone. All 12 gates green after the fix (the SQL
  authorization suite didn't run — this environment has no Docker). Adversarial review of
  the one-line diff came back clean: nothing the app ships touches that folder, no gate or
  script relied on the old behavior, and the reviewer proved the main typecheck still
  covers all 416 of its files with zero dropped anywhere else.
  Second, the answer to backlog #1, and it's the important part: I fetched the live update
  feed and matched it against every committed version of the app bundle. **Your users are
  running the app as of Aug 20, 8:49 AM** (the keyboard-focus fix). Three finished
  improvements have never reached a phone: the health screen honesty fix from later that
  day, the faster day-screen load, and the trust-count fix from Saturday.
- **Found:** the reason nothing newer is live: **the publish key in this cloud environment
  is dead.** Expo's servers reject it outright (permanent "invalid token", not a hiccup —
  I tested it directly, twice, and checked it wasn't a formatting problem). So "ready but
  not shipped — no working publish credentials." Everything else is staged: bundle current,
  gates green, tree clean. The moment a fresh token is in place, any session can publish
  and prove it in minutes.
- **Didn't do:** no database pulse, money pulse, or complaints read — this environment
  still has no Supabase or Stripe credentials (third session in a row to hit this). Left
  the em-dash and blue-ink polish items (#6, #7) for the polish session; they're its lane.
  Backlog #2–#4 all need database access, so they stay parked. No screenshots — nothing
  visual changed. Inbox checked: no OS directives.
- **Recommend:** two five-minute fixes on your side unlock everything: (1) mint a fresh
  Expo access token (expo.dev → Access tokens) and replace EXPO_TOKEN in the cloud
  environment — that un-blocks shipping three finished improvements to real users; (2) add
  the Supabase credentials so the morning pulse and night sentry can actually see. Token
  first: it's real, finished work sitting three days from users' hands.
