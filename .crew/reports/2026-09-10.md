# 2026-09-10

## Error response (overnight sentry, ~11:30 PM ET)

- **What broke:** the AI Nutritionist settings screen (coach-voice) has been crashing
  the moment it opens, for every coach and trainer, since your 6:46 PM publish on the
  9th. The morning scope-cut on the 9th (arrival check-in / injury mode) deleted a
  small list of starter phrases that the AI Nutritionist page still uses; with no build
  step, that's a crash at open time, not a build error. Both of your evening publishes
  carried it. How many users actually hit it, I can't say — still no database
  credentials, so the error monitor's numbers are invisible from here. I found it by
  attacking the live bytes instead: proved by manifest hashes (md5 + sha256, both
  platforms) that what phones run right now is exactly the current tip, then swept
  every registered route in both themes — 302 captures, and this screen was the only
  crash in the app.
- **What I shipped (ready, needs your publish):** restored the deleted phrase list
  exactly as it was, and closed the hole that let it through: the "did somebody forget
  the import?" gate only caught names that still existed somewhere, so a constant
  deleted out from under its uses was invisible to it. It now catches that case too —
  I proved the new rule fails on yesterday's broken tree and stays quiet everywhere
  else. All 16 gates green, the screen renders clean in both themes, rebuilt bundle
  differs from the live one in exactly one file (the fixed screen), nothing rides
  along. Adversarial review confirmed the restore is byte-identical, checked all five
  scope-cut commits for other dangling deletions (none), and its one real finding — an
  unfilled placeholder in this very report — is fixed. NOT live yet: the publish token
  is still dead ("bearer token is invalid"), so one publish from your PC ships it.
- **Also checked tonight:** score v3 and the perfect-plate burst are live and survived
  the same sweep and gates clean — the full v3 audit stays tomorrow's #1. The score
  ceiling migration (0228) only loosens if unapplied; it can't crash anything. Your
  inbox: no unread OS messages.
- **A false alarm of my own, caught before it reached you:** this sandbox's clone was
  five days stale and my first draft claimed the crew went dark after Sep 4. It
  hadn't — the push bounced, the rebase pulled in the week's real history, and the
  claim died here. Written down because the charter says no face-saving.
- **Recommend:** publish from your PC tonight if you can — every coach who opens AI
  Nutritionist crashes until then. And the same standing ask, tonight with teeth:
  read-only Supabase credentials would have shown me in one query how many coaches hit
  this; a fresh EXPO_TOKEN would have had the fix live hours ago.
