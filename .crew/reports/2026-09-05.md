# 2026-09-05

## Error response (05:36 ET)

(Report of record per the standing path — the Drive connector still can't edit an
existing Doc's content; re-tested this session: `update_file` only renames/moves.)

- **What broke / how many users:** unknown — seventh sentry in a row with no database
  credentials in the sandbox (`SUPABASE_ACCESS_TOKEN`/`SUPABASE_DB_PASSWORD` absent, no
  anon read path). If the monitor fired on a real spike, this session cannot see it.
- **Timeline correction:** the entry below labeled "~05:30 ET" was actually the 11:30 PM
  sentry — its commit landed 23:39 ET Sep 4 (03:39 UTC). Its container clock was UTC and
  it mislabeled itself. I'm the real ~05:30 AM run.
- **What I checked:** founder inbox — no unread `OS:`/`OS?` messages from either founder
  address. Git — zero code commits since the 23:39 sentry's all-13-gates-green
  verification of `71aee61`; the only commit since is that sentry's own report file, so
  the verified tree and today's tree are identical and re-running the suite would prove
  nothing new. Skipped it deliberately to end cheaply.
- **Worth remembering (re-tested, not assumed):** `eas whoami` says "The bearer token is
  invalid" — ninth session with a dead publish token. Live users are still on your last
  PC publish, so any real spike traces to already-live code or the backend, not this
  week's unpublished commits.
- **Shipped:** nothing — nothing visible to fix.
- **Recommend:** same as every night: read-only Supabase credentials in the cloud
  environment, or error-response sessions stay blind to the exact spikes they exist for.

## Error response (~05:30 ET)

(For the top of the Daily Ops doc; the Drive connector still can't edit an existing
Doc's content, so per the standing path this repo entry is the report of record.)

- **What broke / how many users:** unknown — still no database credentials in this
  sandbox (no `SUPABASE_ACCESS_TOKEN`, no `SUPABASE_DB_PASSWORD`), and as the last
  sentry established, the proto's own Supabase URL/key are injected by the native shell
  at runtime, so there's no anon read path either. Sixth sentry in a row in the same
  sealed room. If the monitor fired on a real spike, I cannot see it from here.
- **Founder inbox:** checked — no unread `OS:`/`OS?` messages from either founder address.
- **What I checked instead (new commits since the last green sentry run):** last night's
  polish session landed four commits after the 03:53 sentry, so instead of ending on
  trust I re-ran the full suite on today's tip (`71aee61`): **all 13 gates green** (after
  an `npm install` — the first run's 4 failures were just this fresh container missing
  node_modules, not code). I also rebuilt `assets/proto.zip` from the tree and it came
  out byte-identical to the committed one — no repeat of Thursday's stale-zip incident.
- **Worth remembering:** the publish token has been dead for eight sessions, so nothing
  new has reached live users since your last PC publish anyway — a live error spike
  today would trace to the already-live code or the backend, not to this week's
  unpublished commits.
- **Shipped:** nothing — nothing visible to fix.
- **Recommend:** unchanged from every night this week: read-only Supabase credentials
  in the cloud environment, or the error monitor's spikes stay invisible to the very
  sessions fired to answer them.

## 8 AM Build

(Report of record per the standing path — the Drive connector still can't edit an
existing Doc's content; and its upload tool only takes content typed inline, which is
no way to move screenshots, so today's proof images live in this repo at
`.crew/reports/2026-09-05-shots/`, viewable right on GitHub.)

- **Morning pulse:** blind again — no database credentials, no Stripe key in this
  sandbox, so no signups/meals/complaints numbers. Inbox checked: no unread OS:/OS?
  messages from either of your addresses.
- **Shipped:** the homepage now claims our sharpest market position out loud —
  **"Accountability without calorie-counting."** A new section on onstandard.app's
  homepage (between the score and plan styles), a matching FAQ entry (with the
  structured data Google reads), a "NOT A CALORIE COUNTER" block in the App Store /
  Play listing draft, and a design red line in PRODUCT.md so the position is binding
  on future screens: calorie math is never the athlete's homework, food is never
  moralized, and the no-numbers Intuitive style must always exist. This was the
  Friday scout's #1 shippable finding: the big trackers are taking an
  eating-disorder backlash over mandatory counting, and we are structurally the
  alternative. Proof: all 13 verify gates green on the final tree; the landing
  site's own integrity check (three screen widths, every image, every link, zero
  script errors) comes back all clean; screenshots of the final section are in the
  shots folder. The app itself didn't change, so there's no app update to publish —
  the section goes live with your next site deploy from the PC (cloud sessions are
  barred from deploying the production site, by our own rules). Publish token for
  the app is still dead per this morning's sentry, but nothing here needed it.
- **Found:** the adversarial reviewer earned its keep today — my first draft of this
  copy overclaimed in five places, and each one is now reworded to what the code
  actually keeps (worth knowing: the app DOES have a food-search fallback and a
  typed-label path; morning weigh-ins are a built-in default, not coach-scheduled;
  and the Intuitive style's number-hiding leaks on two screens — the pre-log
  analysis and food search show calories to everyone). The same review caught that
  two OLD lines on the site make the exact overclaims I removed ("no typing, no food
  database"), and the dietitians page claims hydration scoring the engine turned
  off. All filed in the backlog for the audit and polish sessions with file/line
  receipts.
- **Didn't do:** the hand-walk audit of your nav/composer work stays with the 1 PM
  session (that separation is the point); the two DB migrations stay parked (still
  no credentials); no "try it" preview link (no Cloudflare token); the SQL
  authorization suite didn't run (no Docker in this sandbox — nothing SQL changed
  anyway).
- **Recommend:** deploy the site from your PC (`cd web/landing-src/deploy && npx
  wrangler deploy`) so the section goes live — and when you get a minute, the two
  Intuitive calorie leaks above are exactly the kind of small dishonesty the new
  red line exists to burn down.

## 1 PM Audit + Fix

(Report of record per the standing path — re-tested this session: the Drive connector's
update tool still only renames/moves, it cannot edit the Doc's content.)

- **Inbox:** checked first — no unread OS:/OS? messages from either of your addresses.
- **This morning's ship, scrutinized:** the calorie-counting section is solid and every
  claim held up against the code. Three problems found and fixed around it: the new
  section (and the phone-thread section above it) had zero breathing room on the
  homepage because the padding rule lived in a stylesheet the homepage never loads —
  the morning session's own screenshot shows the heading jammed under the nav bar; two
  OLD lines on the site still made the exact overclaims the new section retired ("no
  typing, no food database"); and the dietitians page promised hydration scoring the
  engine turned off weeks ago, plus an "awareness is scored on answering" line that is
  wrong twice over now. All rewritten to what the code actually does. Site integrity
  check: all 18 page/width combos clean.
- **The overdue audit (your nav + composer work from 09-03):** walked it like a user —
  really walked it: synthetic finger on the real app, eight gesture scenarios, plus two
  adversarial reviews and a 141-screen sweep (zero errors, zero overflow). The work is
  genuinely good — swipe-back, the pager, the glass, reduced-motion all held. But fresh
  eyes found real bugs, now fixed: a second finger landing mid-swipe left the screen
  stuck half-dragged over a dead strip that ignored taps; two quick back-flicks skipped
  a screen (popped twice); a chat refresh landing mid-swipe yanked the screen out from
  under the finger; the collapsing title flickered at one exact scroll spot; the Plan
  pager did nothing on short tabs and jumped to the wrong tab on old bookmark links;
  and typing in Japanese or Chinese, hitting Enter to pick a character SENT the
  half-typed message in four of five chat boxes. All verified fixed by re-walking.
- **The red line, enforced:** yesterday's "not a calorie counter" rule named the places
  the app still showed calories to Intuitive athletes. All closed today: the pre-log
  check screen, food search, barcode results, and the saved-meals list now show no
  numbers on Intuitive (before/after screenshots in the shots folder). The numbers are
  still computed and stored — coaches and dietitians see everything, always.
- **Honesty fixes beyond the app:** the weekly coach digest had a bug where a database
  hiccup would push digests to coaches who opted OUT of notifications — the failure
  now stops that run instead of guessing. And the quiet-hours setting promised a pause
  that team-standard reminders don't actually honor; the copy now says exactly what
  pauses. The real fix (server honoring quiet hours) is parked with a plan.
- **Shipped:** all of the above — 13/13 gates green, zip rebuilt with an exact
  entry-by-entry proof (10 files changed, nothing riding along), pushed to master.
  **Ready but not shipped to phones: the publish token is still dead (tenth session).**
  The server fixes also need a function deploy from your PC.
- **Didn't do:** the digest-timing bug for coaches east of UTC+7 (needs a DB migration —
  no credentials); the notched-phone glass edge (needs a real device); the sweep's
  "thin screen" flag on the parent link screen is a false positive (it's a real,
  honest, tiny screen). All parked in the backlog with evidence.
- **Recommend:** deploy the site and the edge functions from your PC, and get me a
  working publish token — three days of real fixes are stacked and none of it has
  reached a phone.
