# 2026-09-02

## 1 PM Audit

(For the top of the Daily Ops doc, under today's 8 AM entry; the Drive connector still
cannot edit an existing Doc — re-checked this session, its update tool still only accepts
a new title and folder.)

- **Shipped (ready, riding the next publish):** caught and fixed a bug in what shipped
  this morning, before it ever reached a phone. The new "keep the advice whole" change to
  the AI Nutritionist had a flaw in how it finds sentence endings: any number with a
  decimal point made it silently THROW AWAY the words before it. "Aim for 3.5 oz more
  chicken at dinner" would have reached the athlete as "5 oz more chicken at dinner" —
  and decimals are everywhere in portion advice. The same flaw sat in the server copy of
  the message builder and in the code that removes a deleted food from the AI's text
  (there it left broken fragments like "Aim for 3." behind). All three fixed, with tests
  that replay the exact failure so it cannot come back. All 13 gates green, zip rebuilt,
  entry-by-entry proof: 1 file changed, nothing else rode along. NOT live yet: the
  publish token is still dead from the cloud (re-proved: "bearer token is invalid"), so
  this rides the next publish from your PC, same as the morning's work.
- **Found (and how it got past):** the morning session's own tests only checked plain
  sentences — no decimal ever crossed the new code path. The claim "3.5 oz does not
  split" in its comments was never actually exercised. My tests now pin it.
- **Walked the app fresh (athlete then coach):** the machine sweep of all 141 screens
  came back clean — zero errors, zero broken layouts, zero contrast or tap-size flags.
  The one "thin screen" flag is the same parent invite form the overnight sentry already
  cleared: a genuinely short form, not a defect. Yesterday evening's heading work was
  re-judged with fresh eyes and holds — including the three labels deliberately left as
  non-headings; the reasoning is correct browser behavior. The morning's roster-read
  rework (one server call instead of chunked reads) survived scrutiny: the fallback is
  properly guarded, the no-roster path untouched, failures still surface as failures.
- **Didn't do:** no live publish (dead token) and no database work (no credentials —
  0210 and 0214 stay parked and sequenced as the backlog says). The server half of
  today's fix (meal-opener.ts) also needs an edge-function deploy from a credentialed
  machine; until then the live server still runs the pre-morning code, which is fine —
  the morning's server changes never deployed either, so nothing live carries the bug.
- **Recommend:** publish from the PC when you can — the OTA now carries the morning's
  meal-read upgrade PLUS this fix; without the fix the upgrade actively garbles portion
  advice. Deploy the edge functions in the same sitting.

## 8 AM Build

(For the top of the Daily Ops doc; re-tested the Drive connector this session — its
update tool still only accepts a new title and folder, so the entry lives here.)

- **Pulse:** blind again — no database, Stripe, or feedback credentials in this sandbox,
  and the publish token is still dead (re-proved: "bearer token is invalid"). No unread
  founder emails. You shipped the Roll Call from your PC overnight and pushed three more
  commits this morning mid-session; I rebased onto them and all gates stayed green.
- **Shipped (ready, riding the next publish):** the dietitian board no longer has a
  scaling cliff waiting in it. To show the review queue, the app was asking the server
  for up to 400 meals PER GROUP of 60 athletes and merging them on the phone — a
  240-client roster would haul roughly 1,600 rows over the wire to display 6, and past
  ~200 athletes the request literally stops fitting in a URL. Now one server call does
  the whole thing: the database keeps only the newest rows and sends them once. Nothing
  changes visually (17 screen captures byte-identical before and after) and nobody is
  hurt if you apply the database half late — the app quietly falls back to the old way
  until the new function exists, and I proved both orders work. The database half is
  authored and parked (0214), same credential blocker as the other two.
- **The bigger win under it:** this sandbox can now prove database changes locally. No
  session since Aug 27 could run the SQL authorization suite here (no Docker daemon), so
  I built a throwaway Postgres from the installed binaries with a small Supabase shim,
  applied all 214 migrations from scratch, and got all 7 suites green — plus direct
  attack probes on the new function (a coach asking for a stranger's athlete gets
  nothing, an anonymous caller is refused outright, a rigged request for a million rows
  is clamped). Every future session can now prove SQL before asking you to apply it.
- **Found:** my pre-push attack review caught a real bug in my own SQL — sending the
  same athlete id twice would have duplicated every one of their meals in the queue and
  double-counted their protein in the fueling table. Fixed and proven fixed before
  anything was pushed. It also caught that two test stubs answer unknown server calls
  with a fake "success, empty" — the exact lie the fetcher contract exists to prevent;
  both stubs now model the real server for the new call.
- **Didn't do:** no OTA publish (dead token — the work rides the next publish from your
  PC, and it is safe to publish before OR after applying 0214). No Drive screenshots:
  the images are byte-identical to what users see today, which is the proof itself. The
  inbox's separate roster-wide read (20 newest meals with no athlete filter) still takes
  the slow database path — smaller harm, noted for a follow-up.
- **Recommend:** next publish from the PC picks up this change for free. When database
  credentials show up: apply 0210 (after an OTA, as sequenced in the backlog), then 0214
  (any order, proven safe both ways).

## Error response (overnight, ~5:30 AM)

(For the top of the Daily Ops doc; the Drive connector still cannot edit an existing
Doc's content — re-checked this session, its update tool still only accepts a new title
and folder.)

- **What broke:** unknown — the same blind spot every sentry hits. This sandbox has no
  database credential, so I cannot read the live error events to confirm, size, or
  locate any spike. You already have the email asking for a read-only key; not
  re-sending it.
- **Why this run mattered anyway:** you shipped the Wake-Up Roll Call from your PC
  overnight — last commit 2:08 AM, OTA published 2:10 AM. So real phones have been
  running roughly six thousand new lines for a few hours. If a monitor fired, that code
  is the prime suspect, so I attacked it with everything visible from here.
- **What I proved (all clean):**
  1. The live update feed's bundle fingerprints (both md5 and sha256, checked on the
     production channel) exactly match the bundle committed at the tip of master — so
     what I tested is byte-for-byte what phones are running.
  2. All 12 verify gates green on that exact code.
  3. The full headless walk of the app — 141 screen captures across every registered
     route, including the new Roll Call board, lock-screen flow, coach wake-up editor,
     and the nutrition chat — produced **zero** JavaScript errors, zero failed screens,
     zero layout overflows. The one "thin screen" flag (parent invite-code form) is the
     sweep's heuristic misreading a genuinely short form; it renders fine, no errors.
- **Honest limit:** that walk runs against seeded data and a stubbed backend. It proves
  the shipped screens don't crash at click time; it cannot prove the new server pieces
  (the two roll-call migrations, the reminder/escalation functions, the meal-chat
  memory) behave against the live database. Errors there would only show in the events
  table I can't read.
- **Shipped:** nothing — nothing visible was broken, so the right move was not to touch
  a 3-hour-old release blind.
- **Recommend:** same one thing: a read-only database credential for these sessions.
  Last night is exactly the scenario the overnight sentry exists for, and it ran blind
  on the only question that matters.
