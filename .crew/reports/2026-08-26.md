# 2026-08-26 — Error response

(Belongs at the top of the Daily Ops doc under "Error response"; lives here because the
Drive connector still cannot edit an existing Doc's content — re-tested this session, its
update tool still only renames and moves files.)

- **What broke:** unknown — the **fifth** session in a row without eyes. The cloud env
  still has no Supabase credential, so if the error monitor fired on a real spike, nobody
  here can see which error, which screen, or how many users. (Yesterday's session freshly
  confirmed the app's public key is correctly refused by the database, so there is no
  fallback read path.)
- **How many users it touched:** cannot be known from here.
- **What I could establish, cheaply:** the live production update is still the one from
  **Aug 20** (fetched the live manifest just now: update id 01a01f3b, created
  2026-08-20 12:53 UTC). The Expo token in the env is still rejected by Expo's API
  ("the bearer token is invalid" — re-proved this session). So users are running
  six-day-old code, and no recent commit — including this morning's big polish pass —
  can have caused a live spike, because none of it has reached a phone. Any real spike
  is most likely a bug already fixed in the repo since Aug 23 that cannot ship.
  Founder inbox: no unread OS directives.
- **What I shipped:** nothing — no code changed, no gates run (nothing to gate).
- **New this session:** I searched sent mail and found that **no session has ever
  emailed the founder** — no "OS?" question and no daily digest exists in the last week.
  Every warning about the dead Expo token and the missing Supabase credential has lived
  only in the repo and in a Google Doc no session can write to. Five blind firings plus
  a stranded six-day backlog of fixes crosses the "serious" bar, so this session sent
  the one permitted founder email (subject "OS? incident: …") asking for the two keys.
- **Recommend:** unchanged, and now in his inbox: (1) a fresh Expo token so six days of
  finished fixes reach phones; (2) a read-only Supabase credential so error sessions
  stop running blind.

# 2026-08-26 — 8 AM Build

(Belongs in the Daily Ops doc under today; lives here because the Drive connector still
cannot edit an existing Doc's content — re-tested this session, its update tool still
only renames and moves files.)

**Morning pulse:** blind again, one line each as the charter asks: no Supabase
credential (no signups/meals/error counts), no Stripe key (no money pulse), and the
Beta Feedback Board is unreadable without the database. Founder inbox: no unread OS
directives. The Expo token in the env is still the dead one — re-proved against Expo's
API this morning, same "bearer token is invalid". The overnight session's email asking
for both keys is in your inbox; I didn't send another.

- **Shipped (to the repo, not to phones):** operator screens can no longer be forgotten
  when the roster finishes loading. Before: the app kept a hand-written list of which
  screens to wake up when the team data arrived, and every screen missed off that list
  hung on "Loading" forever if you opened it straight from a relaunch. That list already
  burned us once (the standards board, Aug 23). Today I deleted the list and replaced it
  with a rule: any operator screen, present or future, gets woken automatically. Walking
  every screen that reads the roster found four more quiet victims and fixed them: the
  roll call board (hung forever — screenshots in the OnStandard Screenshots folder,
  before "Loading" vs after a real board), its manage list (claimed "Nothing scheduled
  yet" over a coach's real commitments), the trust-pass form (called the athlete "this
  athlete" instead of their name), and the meal thread's Mark resolved button (could
  never work when opened cold). A new automated gate now fails the build if anyone ever
  writes a screen with this bug again. Proof: all 12 verify gates green, the zip rebuilt
  with exactly the six changed files and nothing else, and the screenshot harness
  reproduced the hang before and a clean board after. Commit `adff540`, pushed.
- **Found:** my own first version had a hole — if the roster load FAILED, two screens
  showed a spinner forever with no way to retry. The adversarial review caught it before
  the push; the fix reuses the honest "Can't reach your team / Try again" screen the
  standards board already had, and I proved it with a simulated dead network.
- **Didn't do:** publish the OTA — the Expo token is still dead, so this is "ready but
  not shipped — no publish credentials," same as every fix since Aug 20. Users are seven
  user-facing commits behind. Also skipped: the SQL authorization suite (the sandbox has
  Docker but no Supabase CLI), and the two live-database security items in the backlog
  (#2 age-editing, #3 score-faking) — both need database credentials to even verify.
- **Recommend:** the same two keys the sentry emailed you about, in this order: a fresh
  Expo token (a week of finished fixes is stranded), then a read-only Supabase
  credential. Once the token works, publishing is a five-minute job for any session.
