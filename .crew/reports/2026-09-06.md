# 2026-09-06

## 8 AM Build

(Report of record per the standing path.)

- **Morning pulse:** blind — no database or Stripe credentials in the cloud env
  (credential streak: 13 sessions through this morning; backlog note updated). Inbox
  checked first: no unread OS:/OS? messages from either of your addresses. Publish token
  got its one cheap check: still "bearer token is invalid." Worth knowing: you pushed two
  commits from your PC at 7:34 and 7:39 this morning (the design-token pass and the
  notification migrations 0220 + 0221) — that clears two parked items, and the 1 PM
  session will give your code its fresh-eyes walk.
- **Shipped (ready for phones, not yet on them):** the "not a calorie counter" promise now
  holds everywhere in the app, including the past. An athlete on the Intuitive style had
  four places left where the app would read stored calorie and protein numbers back to
  them: opening a saved usual meal to edit it (the form arrived pre-filled with the
  numbers), the "save this as a usual?" suggestion on Plan, the goal panel's derived
  targets row, and — the one that mattered most — any PAST meal opened from history or a
  coach-comment notification, which showed the full nutrition row even though the same
  meal hid it on the day it was logged. All four are closed. The numbers are still
  computed, stored, and sent — coaches and dietitians see everything, always; an athlete
  editing a saved meal on Intuitive now fixes the name or place and the stored numbers
  pass through untouched. Typing numbers stays where the athlete is transcribing a label
  they're holding — that's the one fallback the red line allows. Proof: all 14 verify
  gates green; a new test file (9 tests) pins every gate; zip rebuilt LAST with its
  version stamp, entry-by-entry scope proof says exactly 3 files changed, 0 added,
  0 removed; before/after screenshots in `.crew/reports/2026-09-06-shots/` (the BEFORE
  shot shows the edit sheet handing an Intuitive athlete 780 kcal).
- **Found:** the adversarial reviewer earned its keep again — my first pass missed the
  past-meal view entirely (it caught it), caught that a coach can turn off calories alone
  so each number needs its own switch (fixed per-figure), and found a narrow race where
  editing a just-deleted saved meal could have written back a nameless all-zeros
  duplicate (now refused with an honest error). Two small things parked for the polish
  session: the goal panel's explanation line still says "calorie target below
  maintenance" to Intuitive athletes who can never see that target, and the QC seed for
  Intuitive threads writes its sample AI message in a numbers voice a real Intuitive
  analysis wouldn't use.
- **Didn't do:** no OTA publish — ready but not shipped, no publish credentials (13th
  session; the stacked pile from 09-03 onward all rides the moment a fresh EXPO_TOKEN
  lands). Didn't touch Guided's calorie tile (that style shows numbers by design) or the
  label-entry screen (transcription, allowed). No SQL suite run (no Docker here; nothing
  SQL changed).
- **Recommend:** unchanged and now four days loud — one credentialed sitting (fresh
  EXPO_TOKEN + Supabase keys, the 08-26 OS? email) publishes four days of finished,
  proven work in one go.

## Error response (morning sentry, ~5:30 AM)

(Report of record per the standing path.)

- **DB check:** still blind — no Supabase credentials in the cloud env, so whether real
  users are hitting errors right now is unanswerable. Credential streak: 12 sessions
  through 09-06 (backlog note updated in place). EXPO_TOKEN got its one cheap check:
  still "bearer token is invalid."
- **Re-proved the new tree:** one real commit landed since the last sentry-proven tree —
  the 23:57 a11y/sweep follow-up (switch tap targets, "Loading Loading" skeleton fix,
  sweep learns .sr-only), zip rebuilt with its version stamp in the same commit.
  `npm run verify` on current master (5259bd4): **all 14 gates green.**
- **Shipped:** nothing — no visible error, no fix to make, publish token dead anyway.
- **Recommend:** unchanged — one credentialed sitting (fresh EXPO_TOKEN + Supabase keys,
  the 08-26 `OS?` email) restores sight and clears the whole blocked pile.

## Error response (overnight sentry)

(Report of record per the standing path.)

- **DB check:** blind again — no Supabase credentials in the cloud env, so the one
  question this session exists to answer (are real users hitting errors right now?)
  is unanswerable. Credential streak: 11 sessions through 09-06 (backlog note updated
  in place; no re-diagnosis). EXPO_TOKEN got its one cheap check: still "bearer token
  is invalid."
- **Re-proved the new tree:** seven real commits landed since the last sentry-proven
  tree (the 09-05 8 AM landing work through the 23:22 audit fix pass, plus the Sunday
  meta). `npm run verify` on current master (80b2e7c): **all 14 gates green** — the
  count grew from 13 to 14 (a new spacing gate landed with the fix pass); trusted
  verify's own summary and corrected the backlog note that still said 13.
- **Shipped:** nothing — nothing to ship. No error visible, no fix to make, publish
  token dead anyway.
- **Recommend:** unchanged — one credentialed sitting (fresh EXPO_TOKEN + Supabase
  keys, the 08-26 `OS?` email) restores sight and clears the whole blocked pile.

## Sunday META — Week in review (2026-09-01 → 09-05)

(Report of record per the standing path — the Drive connector still can't edit the Ops
doc's content; `update_file`'s schema takes only title/parent, re-confirmed from the
schema this session. This entry is written to sit at the top of the doc the day that
changes.)

**Grade: B+.** Strong week of real shipping with the discipline mostly holding — and
too much time burned on ritual.

**What was high-value (keep picking like this):**
- Tuesday's audit caught the decimal bug before it reached a phone: the morning's
  meal-advice upgrade silently threw away the words before any decimal number, so "Aim
  for 3.5 oz more chicken" would have arrived as "5 oz more chicken." The morning
  session's own tests never exercised a decimal; the audit's now pin it. This is exactly
  why the 1 PM session exists.
- Saturday's audit of the 09-03 nav/composer work — the week's best session. It really
  walked the app (synthetic finger, eight gesture scenarios) instead of trusting the
  clean machine sweep, and found what the sweep never would: a second finger mid-swipe
  stranding a dead half-dragged screen, two fast back-flicks skipping a screen, Enter
  during Japanese/Chinese typing sending half-composed text in four of five chat boxes.
  Also caught the weekly digest pushing to opted-out coaches whenever one database read
  hiccuped — a trust bug, fixed so the failure stops the run instead of guessing.
- Wednesday's quiet multiplier: a session built a throwaway local Postgres rig so SQL
  can be proven by attack in-sandbox. Thursday's inbox RPC immediately used it to prove
  the obvious implementation would have re-opened July's private-notes leak — and shipped
  the safe version with a permanent attack probe instead.
- The Friday scout → Saturday pipeline worked end to end: scout files "rivals are taking
  an eating-disorder backlash over mandatory counting," and by Saturday morning the
  homepage claims "accountability without calorie-counting" — with every line
  adversarially reworded down to what the code actually keeps, a binding red line in
  PRODUCT.md, and the app's own leaks against that claim closed the same day.

**What went wrong (and what now prevents it):**
- Thursday's 8 AM session built the app bundle BEFORE its final review fixes landed, so
  the committed bundle was missing its own corrections — a publish that day would have
  shipped the unreviewed version. The 7 PM session caught and repaired it, but this was
  the week's closest call. "Build the zip LAST, commit the version stamp with it" is now
  a charter gotcha, not a backlog footnote.
- The founder's four nav/composer commits sat live TWO DAYS with no audit because no
  1 PM session fired Wednesday and Friday's slot goes to the scout. The sessions did
  track the debt in the backlog, but the charter now says it outright: a missed slot's
  duty becomes backlog #1, dated, and the next audit session pays it first.
- Ritual ate real time. Every report re-tested and re-narrated the same Drive-connector
  limitation in nearly identical paragraphs; early overnight sentries re-ran full gate
  suites and 141-screen sweeps on trees that hadn't changed a byte, and ten sessions in
  a row re-diagnosed the same dead publish token. The charter now caps all three: the
  7 PM session alone schema-checks Drive once a day, sentries end cheaply on an
  unchanged tree, and a known-dead credential gets one check plus a streak citation.
- Small taste slips, all caught in-house: the meal reward chip wearing alarm red most of
  the day (fixed Thursday), "You're OnStandard" vs "You're on standard" voice drift, two
  em dashes in server copy. Nothing the founder had to correct by hand — in fact no OS:
  correction email arrived all week, which is either trust or inattention; the sessions
  earned the benefit of the doubt this week.

**The one thing that needs the founder:** the 08-26 `OS?` email asking for two keys is
eleven days unanswered. The publish token has been dead for ten straight sessions, every
overnight error-response session ran blind on the one question it exists to answer, and
three days of finished, proven work (tap targets, the honest credit chip, the gesture
and IME fixes, the Intuitive leak closures) is stacked behind one credential. One
credentialed sitting — or fresh keys in the cloud env — clears the entire pile:
publish, edge functions, site deploy, migrations 0210/0214/0219.

**Charter changes this session (mutable sections only; Red lines and the numbered
Shipping discipline rules preserved verbatim — diff-checked):** audit-debt rule, sentry
cheap-exit rule, credential-streak rule, Drive-ritual cap, and four new hard-won
gotchas (zip-last + version stamp, isComposing on Enter-send inputs, attack probes for
SECURITY DEFINER RPCs, missed-slot debt). Backlog compressed and reranked: Intuitive
red-line completion and the M4 parent-view spec lead the sessions' own queue; the
founder-blocked pile is #1 and clearly labeled his.

**Red-line change proposals: none.** Nothing this week argued for weakening or changing
a frozen rule.

**Didn't do:** no code touched, no gates run (docs-only change), no email sent (the
meta session isn't the digest and the charter permits no other mail — tonight's 7 PM
digest should point here).
